name = "portfolio"
compatibility_date = "2026-05-01"
compatibility_flags = ["nodejs_compat"]

# Keep the workers.dev URL active; set false once slvlkn.cc is attached as a custom domain.
workers_dev = true
preview_urls = false

# GITHUB_TOKEN and GITHUB_USERNAME are runtime secrets set in the Worker dashboard
# (Settings -> Variables and Secrets), kept out of the repo.
