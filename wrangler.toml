name = "portfolio"
compatibility_date = "2026-05-01"
compatibility_flags = ["nodejs_compat"]

# Served from the custom domain (slvlkn.cc); no workers.dev subdomain or preview URLs.
workers_dev = false
preview_urls = false
